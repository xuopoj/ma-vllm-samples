# Provenance for this deployment. See CLAUDE.md "Deployment provenance".
source: https://docs.vllm.ai/projects/ascend/en/latest/tutorials/models/DeepSeek-V4-Flash.html
derived: 2026-06-27      # re-derived against the v0.21.0rc1 upstream tutorial
vllm: bundled-in-image   # upstream page pins no vLLM version; use the vLLM shipped in the vllm-ascend image
vllm_ascend: quay.io/ascend/vllm-ascend:v0.21.0rc1-a3
verified: false          # refreshed to v0.21.0rc1, NOT re-validated on hardware at this version
notes: '2x A3 mixed engine, DP=8 x TP=4 (a3/1node doubled across 2 nodes). v0.21.0rc1 refresh: migrated speculative method deepseek_mtp -> mtp (enforce_eager: true) and dropped dead env toggles (ACL_OP_INIT_MODE, ASCEND_A3_ENABLE, USE_MULTI_BLOCK_POOL, VLLM_ASCEND_ENABLE_FUSED_MC2, USE_MULTI_GROUPS_KV_CACHE). Was hardware-verified at an earlier image; flipped to false until re-run on v0.21.0rc1.'
